<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Locales
    |--------------------------------------------------------------------------
    |
    | قائمة اللغات المدعومة في المشروع. لازم تكون متطابقة مع اللي بتستخدمه
    | في قاعدة البيانات (مثلاً أعمدة json أو جداول الترجمة).
    |
    */

    'locales' => [
        'en',
        'ar',
        // لو عايز تزود لغات تانية ضيفها هنا
    ],

    /*
    |--------------------------------------------------------------------------
    | Fallback Locale
    |--------------------------------------------------------------------------
    |
    | اللغة الاحتياطية لو الترجمة مش موجودة.
    |
    */

    'fallback_locale' => 'en',

    /*
    |--------------------------------------------------------------------------
    | Use Fallback
    |--------------------------------------------------------------------------
    |
    | لو true: بيرجع fallback_locale لو الترجمة ناقصة.
    |
    */

    'use_fallback' => true,

    /*
    |--------------------------------------------------------------------------
    | Fallbacks
    |--------------------------------------------------------------------------
    |
    | ممكن تخصص fallback مختلفة لكل لغة.
    |
    */

    'fallbacks' => [
        'ar' => 'en',
    ],
];
